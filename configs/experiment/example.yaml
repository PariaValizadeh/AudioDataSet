# @package _global_
#package global is neccessary!
defaults:
  - override /hardware_config: miniDSP_config.yaml
  - override /experiment_config: default_experiment_meta.yaml
  - override /recorder_config: recorder_config.yaml
 
# Device selection for the experiment
selected_device: "hardware2"  # Use hardware1 by default (this can be overridden in the experiment config)


experiment_config:
  experiment_id: "test1"  # Default experiment identifier
  sample_count: 1
  

recorder_config:
  duration: 3
  